
* Нахожу какой-то датасет с бинарной классификацией
* В качестве метрики выбираю ROC-AUC
* Варьируемые параметры при экспериментах:
  1. Random Seed
  2. Количество слоев
  3. Размерность слоев
* Random Seed влияет на инициализацию весов и на разбиение train-test
* Сравниваются две модели - классическая без регуляризации и байесовская
* Обучение идёт до упора

1. Надо найти датасеты, и прогнать на максимальных размерностях их, чтобы убедиться, что не падаю по OOM
